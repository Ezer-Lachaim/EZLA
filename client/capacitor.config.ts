import { CapacitorConfig } from '@capacitor/cli';

export default {
  appId: 'com.EZLA',
  appName: 'עזר לחיים',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
} satisfies CapacitorConfig;
